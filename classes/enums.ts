"use strict";

export enum Keys {
  Enter = 13,
  Esc = 27,
  Space = 32,
  Left = 37,
  Up = 38,
  Right = 39,
  Down = 40
}

export enum Screen {
  Loading = 0,
  Front = 1,
  Instructions = 2,
  Game = 3,
  GameOver = 4
}

export enum KeyMode {
  Up = 0,
  Down = 1
}

export enum Direction {
  Left = 0,
  Right = 1
}
